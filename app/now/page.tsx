import EmptyState from "@/components/EmptyState";

export default function Page() {
  return (
    <EmptyState
      label="Now — the present is unobserved"
      sub="This page is being written. Check back soon."
    />
  );
}